spring:
  application:
    name: r2l-users-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8006
gateway:
  secret: ${GATEWAY_SECRET:r2l-gateway-secret}